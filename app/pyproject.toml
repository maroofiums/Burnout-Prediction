[project]
name = "burnout-api"
version = "0.1.0"
description = "Burnout Prediction API"
requires-python = "==3.11.*"

dependencies = [
    "fastapi[standard]",
    "uvicorn",
    "pandas",
    "numpy",
    "scikit-learn==1.6.1",
    "joblib"
]